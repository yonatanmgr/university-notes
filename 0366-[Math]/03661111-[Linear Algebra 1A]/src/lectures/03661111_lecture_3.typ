#import "../template.typ": *

#show: project.with(
  title: "אלגברה לינארית 1א׳ - שיעור 3",
  authors: ("יונתן מגר",),
  date: "09 בינואר, 2024",
)

#set enum(numbering: "(1.א)")

== דוגמאות לשדות
ראינו סוג מסוים של שדות: לדוגמה, $QQ seq RR seq CC$.

=== תזכורת (מהשיעור עם ענת):
נגדיר יחס על $ZZ$ בהינתן $n >=1$ קבוע: $a equiv b mod n <=> n|a-b$, כלומר קיים $c$ שלם כך ש-$a-b=c n$. הוכחנו כי היחס *רקלפסיבי*: $a equiv a mod n$, *סימטרי* *וטרנזיטיבי*. (אשלים בהמשך).

נגדיר את מחלקות השקילות $[x]_n = {y in ZZ | y equiv x mod n}={x, x pm n, x pm 2n dots }$. 

=== למה:
+ כל שתי מחלקות שקילות שוות או זרות.
+ כל איבר שלם נמצא באחת ממחלקות השקילות.
+ זה מגדיר חלוקה של $ZZ$: כלומר, $ZZ = [0]_n uud dots uud [n-1]_n$ (זה אומר ש-$ZZ$ איחוד של מחלקות השקילות $[0]$ עד [$n-1$] ואין חפיפה).

=== הוכחה:
+ יהיו $[a]_n, [b]_n$ מחלקות שקילות. 
  - במקרה א׳, $[a] nn [b] = emptyset$. ניצחנו (מחלקות השקילות זרות).
  - במקרה ב׳, $exists c in [a] nn [b]$. כלומר, $c equiv a mod n or c equiv b mod n$. צ״ל $[a]=[b]$. 
    
    נוכיח ש-$[a] seq [b]$ (משיקולי סימטריה, יינבע גם ש-$[b] seq [a]$): יהי $x in [a]$. כלומר, $x equiv a mod n$, וגם ראינו קודם ש-$c equiv a mod n$. משיקולי סימטריות וטרנזיטיביות, נגרר ש-$x equiv c mod n$. נזכיר ש-$c equiv b mod n$, ומטרנזיטיביות $x equiv b mod n$, כרצוי. #QED
  
+ ברור כי לכל $a in ZZ$ מתקיים $a in [a]$ מרפלקסיביות. #QED
  
+ קודם נוכיח ש-$ZZ=tb(ub, n-1, i=0)[i]_n$. הכיוון $suq$ ברור. עבור הכיוון $seq$, נוכיח: יהי $x in ZZ$ צריך למצוא $0 <= i <= n-1$, כך ש-$x in [i]$. נזכיר כי השלמים $ZZ$ באים עם חילוק עם שארית, כלומר נוכל לבטא את $x$ בתור $a n + r$, כך ש-$x <= r <= n-1$. אז, $x - r = a n$ ולכן $x equiv r mod n$ ומכך $x in [r]$ ($qed$ חלק ראשון).

  _חלק שני_: לכל $0 <= j <= i <= n-1$ מתקיים ש-$[i] nn [j] = emptyset$. לפי (1) די להראות ש-$[i] != [j]$. נראה ש-$i in.not [j]$ למרות ש-$i in [i]$, ואכן $0 <= i-j <=n$. לכן $i-j != a n$ ולכן $i != j mod n$ כלומר $i in.not [j]$. #QED

== הגדרה - $ZZ slash n ZZ$
נסמן ב-$ZZ slash n ZZ$ את קבוצת מחלקות השקילות, כלומר $ZZ slash n ZZ = {[0]_n, [1]_n dots, [n-1]_n}$ קבוצה בגודל $n$.

נגדיר: $[x]+[y]:=[x+y]$, $[x]dot[y]:=[x dot y]$. צריך להראות שהפעולות מוגדרות היטב, כלומר אינן תלויות בבחירת הנציגים.

=== טענה - החיבור והכפל ב-$ZZ slash n ZZ$ מוגדרים היטב
 נוכיח: $x_1, x_2 in ZZ$ כך ש-$[x_1]=[x_2]$ ו-$y_1, y_2 in ZZ$ כך ש-$[y_1]=[y_2]$. צ״ל $[x_1+y_1]=[x_2+y_2]$ וגם $[x_1 dot y_1]=[x_2 dot y_2]$. לפי הלמה הקודמת, די להראות ש-$[x_1+y_1] nn [x_2+y_2] != emptyset$. די להראות ש-$x_1+y_1 equiv x_2+y_2 mod n$ (ואותו דבר עבור כפל). 

ואכן, קיימים $x_1-x_2=x_3 n$ ו-$y_1-y_2 = y_3 n$. ואז, $ x_1+y_1-(x_2+y_2) = x_1-x_2+y_1-y_2 = n(x_3+y_3) $ ולכן הם שקולים. באופן דומה עבור כפל, $ x_1 y_1 - x_2 y_2 = (x_2 + x_3 n)(y_2 + y_3 n) - x_2 y_2 = n[x_3 y_2 + x_2 y_3 + x_3 y_3 n] $ #QED

=== טענה - $ZZ slash n ZZ$ הוא שדה
$ZZ slash n ZZ$ עם החיבור והכפל שהגדרנו ועם $[0]$ כאיבר האפס ו-$[1]$ כאיבר היחידה מקיים את כל התכונות של שדה, פרט אולי לקיום הופכי. נוכיח (חלקית): נגיד ש-$[0]$ הוא באמת איבר האפס: $[x]+[0]=[x+0]=[x]$ כרצוי. כך הלאה עבור איבר היחידה ושאר תכונות השדה. הטענה לכן נובעת מקיום התכונות בשלמים. #QED

=== דוגמאות
+ $n=2$. $ZZ slash 2 ZZ = {"זוגיים", "אי-זוגיים"} = {[0],[1]} = {[8],[-3]}$. זה שדה המזדהה עם $FF_2$.
+ $n=3$. $ZZ slash 3 ZZ = {[0],[1],[2]} = {[0],[1],[-1]}$. זהו שדה (ניתן לזהותו בתור $FF_3$).
+ $n=4$. $ZZ slash 4 ZZ = {[0]_4,[1]_4,[2]_4,[3]_4}$. ל-$2$ אין הופכי, כי $[2] dot [x] = [2 x]$ וכל האיברים ב-$2x$ זוגיים ולכן לא נקבל את $[1]$. דרך אחרת: $[2] dot [2] = [0]$ ובשדה זה לא יכול להתקיים. כלומר, זהו לא שדה.
+ $n=5$. $ZZ slash 5 ZZ$ שדה, $2 dot 3 equiv 1 mod 5$.
+ $n=6$. $ZZ slash 6 ZZ$ לא שדה כי $2 dot 3 equiv 0 mod 6$.

קל להכליל בתור משפט: *$ZZ slash n ZZ$ הוא שדה אמ״מ $n$ ראשוני.* (נדלג על ההוכחה).

= מציין של שדה
ב-$QQ$, $1+1+1+1 dots +1 != 0$. אך ב-$F=ZZ slash p ZZ$, מתקיים $1_F+ dots+ 1_F$ ($p$ פעמים) שווה ל-$0$.
נגדיר באינדוקציה $n dot 1_F := 1_F + dots + 1_F "(n פעמים)"$, ו-$-n dot 1_F = -(n dot 1_F)$. 

נגדיר את המציין של השדה כך: $ char(F):= cases(0 "if" n dot 1_F != 0 forall n in NN, "else" min {n in NN | n dot 1_F = 0}) $


= מערכות משוואות מעל שדות
יהי $F$ שדה כלשהו, ונסמן $1=1_F$ ו-$0=0_F$. 
== הגדרות
+ משוואה לינארית ב-$n$ נעלמים מעל $F$ עם מקדמים $a_1 dots a_n, b in F$ היא משוואה מהצורה $a_1 x_1 + dots + a_n x_n = b$.
+ מערכת משוואות של $m$ משוואות ב-$n$ נעלמים מעל $F$ היא אוסף של $m$ משוואות כמו ב-(1). נרשום: 
$ 
  mat(
    a_(1n) x_1 + dots ,a_(1n) x_n, = b_1;
    dots.v, dots.v, dots.v;
    a_(m 1) x_1 + dots ,a_(m n) x_n, = b_m
  )
$
 כאשר $a_(i j) in F$, $1 <= i <= m$ נקראים המקדמים. (להשלים)
3. פתרון של מערכת המשוואות זה $(x_1, dots x_n) in F ^n$ הוא $n$-יה של איברים ב-$F$ כך שאם נציב את $x_i$ ב-$X_i$ במערכת המשוואות כל המשוואות יתקיימו.
+ קבוצת הפתרונות היא ${(x_1, dots, x_n) in F^n | (x_1, dots, x_n) "הוא פתרון של מערכת המשוואות"}$.

=== דוגמה: $X+Y=0$.
פתרונות לדוגמה, $(0,0)$, $(1,-1)$. קבוצת הפתרונות היא ${(alpha, -alpha) | alpha in F}$.